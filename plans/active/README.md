# Active Plans

Use this folder for temporary plans that are still in motion.

Each plan should state:

- `Status`
- `Owner`
- `Expires when`
- `Promote to`

When a plan finishes:

- archive it in `plans/archived/` if the record is still useful, or
- delete it if it no longer adds value

If the outcome becomes durable policy, promote it into canonical docs.
